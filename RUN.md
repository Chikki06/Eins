# To run:

```
git clone https://github.com/funwolf7/cs128h-final-project
cd cs128h-final-project
cargo run
```

To test with multiple players on one device, run the client in multiple terminals. You can use 0.0.0.0 as the IP. For multiplayer LAN, you may need to disable a firewall or allow the listed port.
